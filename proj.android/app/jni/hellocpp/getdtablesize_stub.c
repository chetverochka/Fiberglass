// getdtablesize_stub.c
#include <unistd.h>
#include <sys/resource.h>

// Заглушка для устаревшей функции getdtablesize(), отсутствующей в современном Android NDK.
// Она использовалась для получения максимального количества открытых файловых дескрипторов.
// Для Android достаточно вернуть разумное число, например, 1024.
int getdtablesize() {
    // getdtablesize() устарел.
    // На современных системах можно использовать getrlimit(RLIMIT_NOFILE)
    // или вернуть разумное значение, как это делали в старых портах.
    return 1024;
}